<%= semantic_form_for [:admin, resource] do |f| %>
  <%= f.inputs "Наполнение" do %>
    <%= f.input :name, input_html: {rows: 1}, label: 'Заголовок' %>
    <%= f.input :content, as: :ckeditor, label: 'Контент' %>
  <% end %>
  
  <%= f.actions %>
<% end %>